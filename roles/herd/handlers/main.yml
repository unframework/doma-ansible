---
- name: Reload systemd user
  ansible.builtin.systemd:
    daemon_reload: yes
    scope: user

- name: Restart herd
  ansible.builtin.systemd:
    name: herd.service
    state: restarted
    scope: user
