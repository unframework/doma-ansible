# Managed by Ansible - do not edit directly
services:
  asterisk:
    image: "{{ asterisk_image }}"
    network_mode: host
    restart: unless-stopped
    volumes:
{% for f in asterisk_config_files %}
      - ./config/{{ f }}:/etc/asterisk/{{ f }}:ro
{% endfor %}
